# 后续主控委派提示词

本文仅供后续用户授权启动时使用，目前不执行。

---

你是本阶段主控开发经理。请落实AI指挥官直连与战术平板核心系统，先阅读仓库AGENTS.md及Docs/AICommander/下PHASE_TASKS.md、DEVELOPMENT.md、EXECUTION.md，使用已认可References中的SVG、图片和提示词。

采用 codex://threads/01a0ca99-e997-7733-9a45-d4ee7b146ff8 的主控模式：主控只写文档、派发、协调与交付核对，代码／Unity操作／Git集成由独立task thread执行。不使用subagent／spawn_agent，不允许嵌套代理。旧UI-01已撤销，不恢复它；本阶段设计工作作为UI-02重新明确派发。

执行任务固定gpt-6-sol、thinking xhigh，要求请求级service_tier priority；按实际能力核验设置，不虚构create_thread不支持的参数，不用prompt文字冒充生效证据。并发上限沿用最新用户授权，按文件独立性尽量并行，不为填满名额创建无价值任务。

最高优先级是速度：不写／不跑自动测试，不建探针／夹具／回归矩阵，不设置中间验收或独立评审轮次。正常Unity导入和编译错误直接修复；最终只保留EXECUTION.md中的一次人工校验。已确认的产品防错逻辑仍要实现，不因省略测试而删除。

先记录最新集成基线及用户未提交修改，不沿用旧P2或Toolkit的固定SHA。AI-00仅冻结网络、快照、会话、支援桥接和暂停的最小接口，然后并行派AI-01、AI-02、AI-03、AI-04、UI-02和UI-03；INT-AI逐步串行合入。UI-03先用原生组件搭结构，不等待美术；UI-02用内置ImageGen补最终资源并交Unity实际使用。

所有工作必须遵守：

- Unity C#直连兼容/v1/chat/completions，首页URL／Key／model，模型ID可配置，暂定DeepSeek V4 Flash不等于已核验具体ID。
- Key仅程序内存；URL／model本机保存；超时默认15秒，未缩放时钟；无自动重试、无流式、无LLM主动查询。
- 状态随请求发送，只提供已知内容及6轮历史。回复仅对白＋无／医疗／弱点解析提案，参数来自Unity合法选项。
- 现有Support执行合同；查看与确认两次校验；玩家确认才执行；真实结果本地追加，不自动再调模型，不重复收费。
- 手机全入口暂停战斗及其计时；UI／网络继续；暂停中可执行支援。不要把Run.Running改成false导致支援拒绝或Boss清场；关闭／重开／最终演出必须正确释放手机暂停。
- 安全节点内不再因敌近禁输入；非安全节点仍可阅读和手动支援。
- 平板按认可像素草图，底部通讯／支援／记录，聊天卡片与合同覆盖层；不添加暂停文字，不把正文烘焙成图。
- 保留Toolkit焦点玩家／多Boss适配、原最终选择和结局手机。共享NativePhone/Hud及场景由INT-AI独占最终写入，Unity只给一个任务使用，禁止旧一次性菜单重建NativeDemo。
- 取消／关闭／重开必须隔离迟到响应；切页不取消请求。已终态提案不能再执行，手动／AI合同不能互相覆盖。

每项执行任务交付提交、修改目录、调用入口和待集成说明即可，无测试报告要求。主控读取实际diff／交付摘要后续派，不反复重跑旧检查，不新建自动唤醒或外部服务。

最终报告给出启动入口、配置方式、已实现功能、资源位置、版本提交与一页人工检查单。人工尚未操作则准确标“待人工校验”，不阻塞代码交付，不声称已通过。此阶段不安排演示流程、讲解脚本、外置手机、全服系统、长期记忆或新的战斗机制。

---

## 单任务消息模板

```text
任务ID与名称：
独立task thread；gpt-6-sol / xhigh / priority，禁止subagent。
基线SHA与工作树：
输入文档及固定接口：
允许修改路径：
不得修改的共享文件：
交付物和调用入口：
Unity使用权限：无／由INT协调唯一时段。
不写自动测试、不设中间验收；交付功能与集成说明。
回报threadId、提交、变更目录、实际阻断和下一依赖。
```
