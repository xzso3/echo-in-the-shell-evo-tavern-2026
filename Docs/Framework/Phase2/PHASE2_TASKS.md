# P2 任务与依赖

本文件与 [实现规范](PHASE2_IMPLEMENTATION_SPEC.md)、[验收清单](PHASE2_ACCEPTANCE.md)、[主控规则](PHASE2_ORCHESTRATOR.md)、[状态台账](PHASE2_STATUS.md)共同定义当前阶段。历史 P1 任务名称只是可复用执行任务的旧标题，不恢复旧流程。

## 依赖顺序

| ID | 任务 | 依赖 | 执行任务 | 完成条件 |
| --- | --- | --- | --- | --- |
| P2-00 | 建立现行方案与台账 | 无 | 主控 | 文档说明基线、范围、状态和验收；只提交本阶段文档 |
| P2-01 | NPC、分支任务与实际 ECA 接线 | P2-00 的范围决定；源码基线 `c8083c1` | 复用 P1-07，ID `01a0c9bb-fdbe-7f62-a3d1-8a80fd0fb604` | 两分支实现于现有场景；代码、场景、说明提交；必要编译/冒烟和未测项写清 |
| P2-02 | 审查和集成 | P2-01 提交 | 复用 INT-00，ID `01a0c881-9454-7fa3-a276-cb5756debad3` | 只导入 P2-01 新提交到交付工作区；保留旧祖先和未跟踪产物；形成新集成 SHA 与交接说明 |
| P2-03 | Windows 人工试玩与必要修复 | P2-02 交付 | 用户试玩；主控按反馈复用对应执行任务 | 记录实际走过的分支与原主线；严重阻断修复后只复验受影响路径 |

P2-01 已于 2026-09-23 派发。P2-02 尚未派发，不得在 P2-01 提交前抢占 Unity Editor。P2-03 的 Windows 构建只由用户手动进行。用户正在确认 P2 除 NPC 以外还包含哪些现行架构目标；确认后先在本文件新增任务卡和依赖，再续派。

## P2-01 任务卡

- 输入：`c8083c1`、现有 NativeDemo、当前 `NativeQuest`、`NativeNarrative`、`NativeDialogue`、`NativeInteraction`、`NativeEcaRules` 及 FusionPixel。
- 工作：在现有场景接入一名 NPC；显示两条互斥可选分支；从真实私人记忆或检修通道事实推进并返回 NPC 提交；用 ECA 协调事件、条件和动作。业务状态按实现规范归属。
- 输出：新 `codex/` 分支及提交、可直接打开的场景、简体中文操作说明、实际检查和未测项。原工作区分支与产物保留。
- 边界：不改结局计分、支援授权、Boss 判定或原主线规则；不制作第二关、通用框架工具或 Windows 构建。
- 验收：开发期只做 [验收清单](PHASE2_ACCEPTANCE.md)中必要的编译/启动/当前阻断检查；其余由用户最后人工试玩。

## P2-02 任务卡

- 只从 P2-01 的新分支选择本轮提交；核对与最新集成基线的差量、场景引用、中文字体和 Build Settings 唯一启用场景。
- 如有冲突或实现缺口，交回 P2-01 修补；不替执行任务重做功能。
- 不混入旧 P1 分支祖先链，不删除历史素材、工作树和 `Tests/Tools` 旧 `bin/obj`。
- 回报新交付工作区 SHA、实际集成检查、人工未测项及面向用户的打开/操作说明。

## 续派规则

同一工作包的修补继续原任务；只有独立、文件边界明确的工作才并行派第二项。任何时候最多两项执行任务、一个 Unity Editor 执行任务。主控只负责文档、调度、核查与验收。下一项在前置提交和证据到位后直接派发，不逐次请求许可。派发或续派统一指定 `gpt-6-sol`、`xhigh`。

Chunk 地图、道具/装备/效果及其他现行架构目标是否进入 P2，待用户明确选择。当前任务表只授权已确认的 NPC 切片，不因历史 P1 列表自动扩张。
