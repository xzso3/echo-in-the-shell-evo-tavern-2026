# P2 优先队列与时间切线

用户于 2026-09-23 确认：P2 继续 `FRAMEWORK_ARCHITECTURE.md` 顶部现行修订，按下表顺序快速实现。**P2 必须在 2026-09-24 11:00（UTC+8）收束；未完成目标原样登记，另开后续阶段。** 本表是优先队列，不宣称七组目标都能在截止前完成。

| 顺序 / 任务 | 最小可玩交付 | 依赖与当前状态 |
| --- | --- | --- |
| P2-01 NPC、任务、对白、ECA | 一个本地 NPC 的两条互斥可选委托，真实记忆/绕行事实决定提交 | 已实现于 `6daddd3`，正交 INT 集成 |
| P2-03 Chunk / Map | 在 `NativeDemo` 扩出一段由至少两个可复用 Chunk 组成的可走区域，端口相接、障碍和门由 Map 维护；原主线仍可通过 | P2-02 集成后，优先占用 Unity 槽 |
| P2-04 Actor / Combat | 在新区域放入一种可区分的敌方行为或攻击；Actor 持生命和移动，Combat 持攻击/伤害，实际交战可见 | 可与 P2-03 做文件边界明确的代码工作，但 Unity 验证只占一个槽 |
| P2-05 道具 / 装备 / Buff | 一件可拾取并实际改变战斗的装备，以及一种有明确起止的即时或持续效果；拥有者、效果和 UI 各自只存一份权威状态 | 依赖 P2-04 的实际战斗接口；旧 CyberCity 实现按收益复用 |
| P2-06 音效 / BGM | `NativeDemo` 有可听的循环背景音乐和至少三类关键动作/事件音效，音量可控；资源来源及许可清楚 | 可提前做资源/代码准备，场景接线需 Unity 槽；无外部素材时允许明确标注的自制占位音频 |
| P2-07 ECA 复用 | 从已经真实使用的 NPC、地图、道具/战斗事件中抽取一处重复规则接线，以 Unity 组件或 Inspector 配置复用至少两处；状态仍由所属模块持有 | 等具体重复出现后做窄范围提取，不先造通用容器、JSON IR 或 Schema 工程 |
| P2-08 记忆 / 叙事 / 支援 | 基于新区域增加一项真实有效的记忆选择或战术支援；成功才记录代价和行为，失败/拒绝不收费，并可在手机看到结果 | 依赖 P2-03 与现有 Narrative/Support；不重算旧结局阈值 |
| P2-09 在线指挥官 | 安全节点可进行一次真正的在线自由通讯；离线预设通讯仍可用，模型不能直接授予支援或改结局；密钥不进入 Windows 客户端 | 该组先做在线指挥官。外置手机、保存恢复排在本组后；需要实际服务、凭据与网络条件，若截止前不具备则记录到后续阶段 |

P2-02 是首次集成 NPC；后续每个可交付切片沿用 INT-00 串行集成，不把前一任务的旧祖先链带入。顺序为用户优先级；用户已将执行任务并发上限提高到 **5**，Unity Editor 同时仍只由一项使用。各项做到可操作的最小功能即交集成，开发期只修编译、启动及当前流程阻断。

首批可并行工作按文件边界拆为：P2-03 独占 NativeDemo 场景/Map/Unity；P2-04 只做敌人、攻击代码与预制体，不写共享场景；P2-05 先做道具、装备、移动效果代码/资源，不改 P2-04 的战斗文件，接线待前置接口落地；P2-06 先做音频素材和管理组件，不写共享场景，待 Unity 槽空闲再接入。INT-00 串行管理集成与已完成工作区收束。依赖未满足时可以先做独立代码，不把未接线的部件说成可玩交付。

## 时间和停工规则

- 2026-09-24 08:00 前持续从队列顶部派发；若一个任务预计不能在人工窗口前交付，先保存分支/提交和可运行集成点，不冒险覆盖当前可玩版。
- 08:00—11:00 不再启动新功能；用户手动 Windows 构建与集中试玩。执行任务只处理崩溃、卡死、严重输入和关键资源缺失，并只复验受影响路径。
- 11:00 汇总集成 SHA、已完成/未测/未完成项；未完成目标进入下一阶段，不将 P2 的任务表写成已实现清单。若用户调整时间或优先级，先更新本表和任务台账再续派。

外置手机与保存恢复保留为 P2-09 的后续候选，不在在线指挥官之前抢占执行槽。独立引擎、双宿主、全玩法 JSON、Schema/能力目录、第二内容包和旧 P1/G1/G2/G3 流程仍不恢复。
